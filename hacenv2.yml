name: deepvoice3
channels:
  - pytorch
dependencies:
  - python=3.8
  - pip
  - pip:
    - moreh-driver==0.2.0
    - torch==1.10.0+cpuonly.moreh0.2.0
    - torchvision
    - torchaudio
    - -e ".[bin]"
